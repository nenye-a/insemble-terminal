# Migration `20200708184935-demo-table`

This migration has been generated by RaymondAnggara at 7/8/2020, 6:49:35 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Performance" ADD COLUMN "demo" text   ;

ALTER TABLE "public"."News" ADD COLUMN "demo" text   ;

ALTER TABLE "public"."Activity" ADD COLUMN "demo" text   ;

ALTER TABLE "public"."OwnershipContact" ADD COLUMN "demo" text   ;

ALTER TABLE "public"."OwnershipInfo" ADD COLUMN "demo" text   ;

ALTER TABLE "public"."Map" ADD COLUMN "demo" text   ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200707155549-user-referral..20200708184935-demo-table
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -56,8 +56,13 @@
   ADMIN
   USER
 }
+enum DemoType {
+  BASIC
+  WITH_COMPARE
+}
+
 model LocationTag {
   id     String          @default(cuid()) @id
   type   LocationTagType
   params String
@@ -195,8 +200,9 @@
   data            PerformanceData[]
   compareData     ComparePerformanceData[]
   polling         Boolean                  @default(false)
   error           String?
+  demo            DemoType?
   createdAt       DateTime                 @default(now())
   updatedAt       DateTime                 @updatedAt
 }
@@ -229,8 +235,9 @@
   data            NewsData[]
   compareData     CompareNewsData[]
   polling         Boolean           @default(false)
   error           String?
+  demo            DemoType?
   createdAt       DateTime          @default(now())
   updatedAt       DateTime          @updatedAt
 }
@@ -277,8 +284,9 @@
   data            ActivityData[]
   compareData     CompareActivityData[]
   polling         Boolean               @default(false)
   error           String?
+  demo            DemoType?
   createdAt       DateTime              @default(now())
   updatedAt       DateTime              @updatedAt
 }
@@ -305,8 +313,9 @@
   businessTag     BusinessTag?
   comparationTags ComparationTag[]
   data            MapData[]
   compareData     CompareMapData[]
+  demo            DemoType?
   createdAt       DateTime              @default(now())
   updatedAt       DateTime              @updatedAt
 }
@@ -323,8 +332,9 @@
   type        OwnershipType
   locationTag LocationTag?
   businessTag BusinessTag?
   data        OwnershipContactData[]
+  demo        DemoType?
   createdAt   DateTime               @default(now())
   updatedAt   DateTime               @updatedAt
 }
@@ -342,8 +352,9 @@
   type        OwnershipType
   locationTag LocationTag?
   businessTag BusinessTag?
   data        OwnershipInfoData
+  demo        DemoType?
   createdAt   DateTime          @default(now())
   updatedAt   DateTime          @updatedAt
 }
```


