# Migration `20200623112359-open-news-init`

This migration has been generated by RaymondAnggara at 6/23/2020, 11:23:59 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."OpenNews" (
    "businessTag" text   ,
    "createdAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00',
    "data" text   ,
    "error" text   ,
    "firstArticle" text  NOT NULL DEFAULT '',
    "id" text  NOT NULL ,
    "locationTag" text   ,
    "polling" boolean  NOT NULL DEFAULT false,
    "updatedAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00',
    PRIMARY KEY ("id")
) 

ALTER TABLE "public"."OpenNews" ADD FOREIGN KEY ("locationTag") REFERENCES "public"."LocationTag"("id") ON DELETE SET NULL

ALTER TABLE "public"."OpenNews" ADD FOREIGN KEY ("businessTag") REFERENCES "public"."BusinessTag"("id") ON DELETE SET NULL
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200619142313-review-tag-ownership-to-contact..20200623112359-open-news-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -223,8 +223,20 @@
   createdAt       DateTime          @default(now())
   updatedAt       DateTime          @updatedAt
 }
+model OpenNews {
+  id           String       @default(cuid()) @id
+  firstArticle String
+  locationTag  LocationTag?
+  businessTag  BusinessTag?
+  data         String?
+  polling      Boolean      @default(false)
+  error        String?
+  createdAt    DateTime     @default(now())
+  updatedAt    DateTime     @updatedAt
+}
+
 model SearchLog {
   id          String       @default(cuid()) @id
   locationTag LocationTag?
   businessTag BusinessTag?
```


