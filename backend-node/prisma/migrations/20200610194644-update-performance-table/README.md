# Migration `20200610194644-update-performance-table`

This migration has been generated by RaymondAnggara at 6/10/2020, 7:46:44 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."PerformanceData" DROP COLUMN "totalSales",
ADD COLUMN "customerVolumeIndex" integer   ,
ADD COLUMN "localCategoryIndex" integer   ,
ADD COLUMN "localRetailIndex" integer   ,
ADD COLUMN "nationalIndex" integer   ;

ALTER TABLE "public"."ComparePerformanceData" DROP COLUMN "totalSales",
ADD COLUMN "customerVolumeIndex" integer   ,
ADD COLUMN "localCategoryIndex" integer   ,
ADD COLUMN "localRetailIndex" integer   ,
ADD COLUMN "nationalIndex" integer   ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200610143418-compare-data-with-tag-id..20200610194644-update-performance-table
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -139,24 +139,30 @@
   tokenEmail String
 }
 model PerformanceData {
-  id          String @default(cuid()) @id
-  name        String
-  avgRating   String?
-  totalSales  String?
-  numReview   Int?
-  numLocation Int?
+  id                  String @default(cuid()) @id
+  name                String
+  avgRating           String?
+  customerVolumeIndex Int?
+  localRetailIndex    Int?
+  localCategoryIndex  Int?
+  nationalIndex       Int?
+  numReview           Int?
+  numLocation         Int?
 }
 model ComparePerformanceData {
-  id          String @default(cuid()) @id
-  name        String
-  avgRating   String?
-  totalSales  String?
-  numReview   Int?
-  numLocation Int?
-  compareId   String
+  id                  String @default(cuid()) @id
+  name                String
+  avgRating           String?
+  customerVolumeIndex Int?
+  localRetailIndex    Int?
+  localCategoryIndex  Int?
+  nationalIndex       Int?
+  numReview           Int?
+  numLocation         Int?
+  compareId           String
 }
 model ComparationTag {
   id          String        @default(cuid()) @id
```


