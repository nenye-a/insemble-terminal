# Migration `20200610205833-polling`

This migration has been generated by RaymondAnggara at 6/10/2020, 8:58:33 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Performance" ADD COLUMN "error" text   ,
ADD COLUMN "polling" boolean  NOT NULL DEFAULT false;

ALTER TABLE "public"."News" ADD COLUMN "error" text   ,
ADD COLUMN "polling" boolean  NOT NULL DEFAULT false;

ALTER TABLE "public"."Activity" ADD COLUMN "error" text   ,
ADD COLUMN "polling" boolean  NOT NULL DEFAULT false;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200610194644-update-performance-table..20200610205833-polling
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -181,8 +181,10 @@
   businessTag     BusinessTag?
   comparationTags ComparationTag[]
   data            PerformanceData[]
   compareData     ComparePerformanceData[]
+  polling         Boolean                  @default(false)
+  error           String?
   createdAt       DateTime                 @default(now())
   updatedAt       DateTime                 @updatedAt
 }
@@ -213,8 +215,10 @@
   businessTag     BusinessTag?
   comparationTags ComparationTag[]
   data            NewsData[]
   compareData     CompareNewsData[]
+  polling         Boolean           @default(false)
+  error           String?
   createdAt       DateTime          @default(now())
   updatedAt       DateTime          @updatedAt
 }
@@ -247,8 +251,10 @@
   businessTag     BusinessTag?
   comparationTags ComparationTag[]
   data            ActivityData[]
   compareData     CompareActivityData[]
+  polling         Boolean               @default(false)
+  error           String?
   createdAt       DateTime              @default(now())
   updatedAt       DateTime              @updatedAt
 }
```


